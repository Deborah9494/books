# Library Management Web Application

This project is a web application for managing a library database. It allows users to manage authors, genres, and books through a dynamic user interface built with JavaScript, PHP, HTML, and CSS.
The application implements CRUD operations (Create, Read, Update, and Delete) using asynchronous requests (AJAX/fetch) without reloading the page. Data is retrieved and updated through PHP endpoints that communicate with the database.
